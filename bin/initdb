#!/bin/bash
docker-compose down --volumes --remove-orphans
docker-compose -f docker-compose-init.yml up -d --build
# wait for init container to stop
docker wait odoo_init
# start instances
docker-compose up -d --build --remove-orphans
